#include <stdio.h>
 
int main() 
{
	int t,n,i,c,k,r;
	scanf("%d",&t);
	while(t--)
	{
		c=0;
		scanf("%d",&n);
		int a[n];
		for(i=0;i<n;i++)
		scanf("%d",&a[i]);
		scanf("%d",&k);
                r=a[k-1];
		for(i=0;i<n;i++)
		{
			if(a[i]<r)
			c++;
		}
		printf("%d\n",c+1);
	}
	return 0; 
} 
